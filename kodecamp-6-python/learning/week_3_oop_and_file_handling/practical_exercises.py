# Practical coding exercises (try these yourself)
# Run: python3 practical_exercises.py

#
# Exercise A: Create a class `Rectangle` with width and height in __init__.
#   Add methods `area()` and `perimeter()` that return numbers.
#
# Exercise B: Write a script that writes three lines to `journal.txt`, then
#   reads and prints them. Use modes "w" and "r".
#
# Exercise C: Save a list of dicts (e.g. friends with name and age) to
#   `friends.json` with json.dump, then load it back and print each name.
#
# Exercise D: Extend Exercise C with a small `Friend` class and functions
#   `friend_to_dict` / `friend_from_dict` like the Book examples.
#

print("Open this file and implement the exercises above in this folder or new files.")
